const API_KEY = ""; // GOOGLE API KEY which we don't have yet.

const getCoordsForAddress = (address) => {
  return {
    lat: 40.7484474,
    lng: -73.9871516,
  };
};

module.exports = getCoordsForAddress;
